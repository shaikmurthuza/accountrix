# KVK FastAPI Integration

FastAPI service to fetch Dutch Chamber of Commerce (KVK) data using:
- Zoeken API
- Basisprofiel API

## Run locally

```bash
pip install -r requirements.txt
uvicorn main:app --reload
